// Javascript não tem função exponencial
// pra isso chamamos o objeto Math e usamos a função Power (.pow)
const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)

console.log(area)
console.log(typeof Math)